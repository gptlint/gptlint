# Example

| Key       | Value               |
| --------- | ------------------- |
| Name      | `example-name`      |
| Prechecks | `['"]axios['"]`, `import\\s+.*from\\s*['"]react['"]` |

This example should parse correctly. It uses 2 regex prechecks.
