// Expects an exact boolean value, doesn't handle truthy or falsy values
function toggleFeature(isEnabled: boolean): void {
  // Only accepts true or false, not truthy or falsy values
  if (typeof isEnabled !== 'boolean') {
    throw new Error('isEnabled must be a boolean')
  }
  // Feature toggling logic
}

// Generated by gpt-4-0125-preview
