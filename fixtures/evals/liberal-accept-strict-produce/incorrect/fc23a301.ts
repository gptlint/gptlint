// Expects a fixed structure for JSON input, doesn't handle variations or additional properties
function parseConfig(configJson: string): void {
  const config = JSON.parse(configJson)
  // Requires exact keys: host, port
  if (!config.host || !config.port) {
    throw new Error('Config must include host and port')
  }
  // Config parsing logic
}

// Generated by gpt-4-0125-preview
