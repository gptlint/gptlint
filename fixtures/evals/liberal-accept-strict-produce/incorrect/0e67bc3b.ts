// Only processes specific file extensions, doesn't handle other valid extensions
function readFile(filePath: string): void {
  // Only reads .txt and .md files
  if (!filePath.endsWith('.txt') && !filePath.endsWith('.md')) {
    throw new Error('Unsupported file type')
  }
  // File reading logic
}

// Generated by gpt-4-0125-preview
