// Accepts any input, ensures output is a non-negative integer
function toNonNegativeInteger(input: any): number {
  const num = parseInt(input, 10)
  if (!isNaN(num) && num >= 0) {
    return num
  }
  // Defaults to 0 for non-numeric or negative inputs
  return 0
}

// Generated by gpt-4-0125-preview
