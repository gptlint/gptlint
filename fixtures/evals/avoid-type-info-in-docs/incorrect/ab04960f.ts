/**
 * Toggles a feature flag.
 * 
 * Takes in flagName which is a string and isEnabled which is a boolean.
 */
function toggleFeatureFlag(flagName: string, isEnabled: boolean): void {
  // This VIOLATES the rule by including type information in the JSDoc comments.
  featureFlags[flagName] = isEnabled
}

// Generated by gpt-4-0125-preview
