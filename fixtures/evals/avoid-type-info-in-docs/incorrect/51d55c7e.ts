/**
 * Updates the user's profile.
 * 
 * @param {string} userId - The user's ID.
 * @param {UserProfile} profileData - The new profile data.
 * 
 * @returns {Promise<UserProfile>} - A promise that resolves to the updated profile.
 */
async function updateUserProfile(
  userId: string,
  profileData: UserProfile
): Promise<UserProfile> {
  // This VIOLATES the rule by including type information in the JSDoc comments.
  return database.users.update(userId, profileData)
}

// Generated by gpt-4-0125-preview
