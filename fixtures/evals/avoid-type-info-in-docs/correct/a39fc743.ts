/**
 * Increments a counter in a database.
 *
 * @param counterId - The ID of the counter to increment.
 */
async function incrementCounter(counterId: string): Promise<void> {
  // This comment adheres to the rule by describing the action without repeating type information.
  await db.query(`UPDATE counters SET value = value + 1 WHERE id = $1`, [
    counterId
  ])
}

// Generated by gpt-4-0125-preview
