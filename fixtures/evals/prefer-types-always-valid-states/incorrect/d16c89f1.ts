// Example 1: Violates the rule by allowing both `isProcessing` and `isComplete` to be true, which is an invalid state.
interface OrderStatus {
  isProcessing: boolean
  isComplete: boolean
  // This design allows for invalid states, such as an order being both processing and complete.
}

// Generated by gpt-4-0125-preview
