function logError(error) {
  if (error.isCritical) {
    // This violates the rule by not using an early return for non-critical errors.
    alertAdmin(error)
    logToFile(error)
  }
}

// Generated by gpt-4-0125-preview
