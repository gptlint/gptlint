function validateUser(user) {
  if (user.age >= 18) {
    // This violates the rule by not using an early return for age validation
    if (user.email.includes('@')) {
      // Nested if without early return violates the rule
      console.log('User is valid')
    }
  }
}

// Generated by gpt-4-0125-preview
