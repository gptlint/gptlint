function calculateDiscount(price) {
  if (price > 100) {
    // This violates the rule because it doesn't use an early return
    const discount = price * 0.1
    return price - discount
  }
}

// Generated by gpt-4-0125-preview
