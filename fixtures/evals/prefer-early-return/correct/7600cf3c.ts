function logError(error) {
  // Early return if no error is provided
  if (!error) return

  // Logging the error
  console.error(error)
  // This adheres to the rule by checking for the absence of an error and returning early.
}

// Generated by gpt-4-0125-preview
