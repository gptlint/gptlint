function isWeekend(date) {
  // Early return if date is not provided
  if (!date) return false

  const day = date.getDay()
  // Returning true for Saturday and Sunday
  return day === 6 || day === 0
  // This adheres to the rule by returning early if no date is provided.
}

// Generated by gpt-4-0125-preview
