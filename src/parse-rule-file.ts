import type * as types from './types.js'

export async function parseRuleFile(_content: string): Promise<types.Rule> {
  // TODO
  return {
    name: 'todo',
    message: 'todo',
    desc: 'todo'
  }
}
